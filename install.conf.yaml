- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev

- shell:
    - 
      command: ./setup-homebrew.zsh
      stdout: true
      stderr: true
